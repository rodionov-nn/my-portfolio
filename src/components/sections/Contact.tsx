import Container from "../ui/Container";

export default function Contact() {
  return (
    <section
      id="contact"
      className="relative min-h-screen flex flex-col items-center justify-center text-center gap-4 bg-gradient-to-b from-white/50 dark:from-black/50 to-transparent"
    >
      <Container>
        <h1>
          Contact
        </h1>
      </Container>
    </section>
  );
}

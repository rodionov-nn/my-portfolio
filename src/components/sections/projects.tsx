import { Container } from "../shared";

export function Projects() {
  return (
    <section id="projects" className="bg-gradient-to-b from-white dark:from-black to-white/50 dark:to-black/50">
      <Container className="flex flex-col min-h-screen items-center justify-center">
        <h1>Projects:</h1>
        <div></div>
      </Container>
    </section>
  );
}
